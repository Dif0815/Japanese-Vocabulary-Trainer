# Startup defaults for Quiz and Training.
# vocabulary must match one of the type values currently present in vocabulary.csv, or use all.
# group must match one of the slash-separated group values currently present in vocabulary.csv, or use all.
#
# Quiz:
[quiz]
ask_type=on
direction=mixed
vocabulary=all
group=all

# Training:
[training]
direction=mixed
vocabulary=all
group=all
reveal_all=off
